if (<%= obj_has_errors(@user) %> == 1) {
	<%= return_js_for_edit_modal('users',{:user => @user}).html_safe %>
}

// Replace table with new table
<%= return_js_for_replacing_collection_partial("#users-table-body",@users).html_safe %>

$('#modal').modal('<%= @show_modal %>');         

